name: Daily Portfolio Snapshot

on:
  # Run daily BEFORE first ETF price sync (before market open) for PREVIOUS day
  # This gives Plaid overnight to sync all of yesterday's transactions
  # Process flow: Sync Plaid → Fetch prices → Create snapshot for yesterday
  # 13:00 UTC = 9:00 AM EDT / 8:00 AM EST (always before 9:30 AM market open)
  schedule:
    - cron: '0 13 * * 2-6'  # Tue-Sat at 9:00 AM EDT / 8:00 AM EST (creates Mon-Fri snapshots)

  # Allow manual trigger for testing
  workflow_dispatch:
    inputs:
      date:
        description: 'Snapshot date (YYYY-MM-DD, optional - defaults to today)'
        required: false
        type: string

jobs:
  create-snapshot:
    runs-on: ubuntu-latest

    steps:
      - name: Check Plaid connections
        run: |
          echo "🔌 Checking Plaid connection status..."

          response=$(curl -s -w "\n%{http_code}" -X GET \
            https://401k.mreedon.com/api/db/plaid \
            -H "X-401K-Token: ${{ secrets.API_SHARED_TOKEN }}" \
            -H "Content-Type: application/json" \
            --connect-timeout 10 \
            --max-time 30)

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          if [ "$http_code" -ne 200 ]; then
            echo "⚠️ Could not check Plaid connections (HTTP $http_code)"
          else
            connections=$(echo "$body" | jq -r '.connections // []')
            count=$(echo "$connections" | jq 'length')

            if [ "$count" -eq 0 ]; then
              echo "::warning::🔴 NO PLAID CONNECTIONS FOUND - Transaction sync will not work!"
              echo "❌ No Plaid connections configured. Please re-link your accounts."
            else
              echo "✅ Found $count Plaid connection(s)"

              # Check for stale connections (no sync in 5+ days)
              five_days_ago=$(date -u -d '5 days ago' '+%Y-%m-%dT%H:%M:%SZ')

              echo "$connections" | jq -c '.[]' | while read -r conn; do
                institution=$(echo "$conn" | jq -r '.institution_name')
                last_synced=$(echo "$conn" | jq -r '.last_synced_at // "never"')

                if [ "$last_synced" = "null" ] || [ "$last_synced" = "never" ]; then
                  echo "::warning::⚠️ $institution has never synced successfully"
                elif [[ "$last_synced" < "$five_days_ago" ]]; then
                  echo "::warning::⚠️ $institution last synced on $last_synced (over 5 days ago)"
                else
                  echo "  ✓ $institution - last synced: $last_synced"
                fi
              done
            fi
          fi

      - name: Sync Plaid transactions
        run: |
          echo "🔄 Syncing Plaid transactions (overnight sync ensures all yesterday's trades captured)..."

          response=$(curl -s -w "\n%{http_code}" -X POST \
            https://401k.mreedon.com/api/sync/transactions \
            -H "X-401K-Token: ${{ secrets.API_SHARED_TOKEN }}" \
            -H "Content-Type: application/json" \
            --connect-timeout 10 \
            --max-time 60)

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          echo "HTTP Status: $http_code"

          if [ "$http_code" -eq 200 ]; then
            echo "✅ Plaid sync completed"
            # Extract summary if available
            json_line=$(echo "$body" | grep -o '{.*}' | tail -n1)
            if [ -n "$json_line" ]; then
              formatted_summary=$(echo "$json_line" | jq -r '.formatted_summary // empty' 2>/dev/null)
              if [ -n "$formatted_summary" ]; then
                echo "$formatted_summary"
              fi
            fi
          else
            echo "⚠️ Plaid sync failed with status $http_code, continuing anyway"
            echo "Response: $body"
            # Don't fail workflow - snapshot uses yesterday's date, transactions already in DB
          fi

      - name: Fetch closing prices
        run: |
          echo "💰 Fetching closing prices from Finnhub (forced)..."

          response=$(curl -s -w "\n%{http_code}" -X POST \
            https://401k.mreedon.com/api/prices/refresh \
            -H "X-401K-Token: ${{ secrets.API_SHARED_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"force": true}')

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          echo "Response: $body"
          echo "HTTP Status: $http_code"

          if [ "$http_code" -ne 200 ]; then
            echo "⚠️ Price fetch failed with status $http_code, continuing anyway"
            echo "📝 Note: Snapshot will use last known prices (better than no snapshot)"
          else
            echo "✅ Closing prices fetched successfully"
          fi

      - name: Wait for database to settle
        run: |
          echo "⏳ Waiting 5 seconds for prices to propagate..."
          sleep 5

      - name: Create daily portfolio snapshot
        run: |
          echo "📸 Creating portfolio snapshot for yesterday..."

          # Calculate yesterday's date (snapshot is for previous day)
          if [ -n "${{ github.event.inputs.date }}" ]; then
            snapshot_date="${{ github.event.inputs.date }}"
          else
            snapshot_date=$(date -u -d 'yesterday' '+%Y-%m-%d')
          fi

          echo "Snapshot date: $snapshot_date"
          body="{\"date\": \"$snapshot_date\", \"source\": \"github-actions\"}"

          response=$(curl -s -w "\n%{http_code}" -X POST \
            https://401k.mreedon.com/api/snapshots/save \
            -H "X-401K-Token: ${{ secrets.API_SHARED_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "$body")

          http_code=$(echo "$response" | tail -n1)
          body_response=$(echo "$response" | head -n-1)

          echo "Response: $body_response"
          echo "HTTP Status: $http_code"

          # Check if successful (200) or already exists (409)
          if [ "$http_code" -eq 200 ]; then
            echo "✅ Snapshot created successfully"
            echo "$body_response" | jq -r '.snapshot | "Date: \(.date)\nMarket Value: $\(.marketValue)\nGain/Loss: $\(.gainLoss) (\(.gainLossPercent)%)\nHoldings: \(.holdingsCount)"'
          elif [ "$http_code" -eq 409 ]; then
            echo "ℹ️ Snapshot already exists for today"
          else
            echo "❌ Snapshot creation failed with status $http_code"
            exit 1
          fi

      - name: Send transaction summary email
        run: |
          echo "📧 Sending transaction summary email..."

          response=$(curl -s -w "\n%{http_code}" -X POST \
            https://401k.mreedon.com/api/emails/send-transaction-summary \
            -H "X-401K-Token: ${{ secrets.API_SHARED_TOKEN }}" \
            -H "Content-Type: application/json")

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          echo "Response: $body"
          echo "HTTP Status: $http_code"

          if [ "$http_code" -eq 200 ]; then
            echo "✅ Transaction email sent successfully"
          else
            echo "⚠️ Email failed with status $http_code, but continuing (non-critical)"
            # Don't fail the workflow if email fails
          fi
